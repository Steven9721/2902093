//COCIENTE ENTRE A Y B.
let a = 5;
let b = 10;
let cociente;

if (b !==0) {
    cociente = a / b;
    console.log("El cociente entre a y b es: " + cociente);

} else {
    console.log("No se puede dividir por cero, digite otro numero :");
}